// import {Card, CardHeader, CardBody, Image} from "@nextui-org/react";
// import {Card, CardHeader, CardBody, Image} from "@nextui-org/card";
import { Card, CardHeader, CardBody, Image } from "@nextui-org/react";

export default function Cards() {
  return (
    <Card className="h-full">
      <Image src="/location.png" width={700} />
    </Card>
  );
}
